[English Version](Softveil_specs_v1_6_en.md)

# 覗き見防止フィルターアプリ「Softveil」基本仕様書

**バージョン:** 2.13
**作成日:** 2026年5月28日
**ステータス:** Phase 15 AI監視モード追加

**変更履歴:**

| バージョン | 変更内容 |
|-----------|---------|
| 1.0 - 2.0 | 初期開発〜Ver 2.0 拡張、AI 覗き見検知実装 |
| 2.1 - 2.3 | パネル適応型最適化、wgpu 移行、SPD 基盤、次世代 SPD 技術（実験的）統合 |
| 2.4 | Phase 6 統合型ステルス・スイッチ。OSテーマ連動、輝度操作、ダーク環境最適化フィルタの統合。 |
| 2.5 | ハイブリッド SPD 制御。自動判定をベースにしたパラメータ手動上書き（Fine-tuning）機能の追加。 |
| 2.6 | Phase 8 ステルス・ライト & サブピクセル・ハック。ライトモード向けコントラスト崩壊と 0.5px 制御の導入。 |
| 2.7 | Phase 9 機械的ベンチマーク統合。評価指標 (Obfuscation Index) の定義。および Stealth Light 開発着手。 |
| 2.8 | Phase 10 動的最適化 & サブピクセル_ハック統合。サブピクセル単位の独立制御と、ベンチマーク駆動の自動パラメータ選定を実装。 |
| 2.9 | プリセット管理システムの実装。ベンチマーク結果に基づく推奨設定の保存、およびユーザー定義プリセットの管理機能を追加。 |
| 2.10 | マルチモニター対応プリセットの洗練。階層型 UI (Presets -> Monitor -> Preset) と、全ディスプレイを対象とした一括ベンチマーク・最適化を実装。 |
| 2.11 | UX 刷新と権限ロジックの堅牢化。グローバルメニューの整理（モニター主導型への完全移行）、および macOS の画面収録権限チェックの偽陽性対策を実装。 |
| 2.12 | 自動最適化の強化と描画品質の改善。ベンチマーク完了後の最適プリセット自動適用、および Stealth Light モードを Subpixel 方式へ一本化。 |
| 2.13 | **AI監視 (Vigilance) モードの追加**。通常時は完全透明（または最小限の干渉）で、カメラに複数人数（覗き見者）を検知したときのみ自動で強力なプライバシーフィルター（SPDプロテクト）を起動するインテリジェントモードを実装。 |


---
...
### 4.11 プリセット管理システム (Preset Management System) [Phase 11]

多様なユースケース（オフィス、移動中、夜間等）に即座に対応するため、フィルター設定一式を「プリセット」として保存・管理する機能を導入する。

1.  **データ構造 (FilterSettings & Preset)**:
    *   `FilterSettings`: フィルター形式、濃度 (Alpha)、強度 (Intensity)、および各種上書きパラメータ（周期、遮蔽率、スクロール速度）を統合したデータ構造。
    *   `Preset`: 名前（例: "Office", "Transit"）と `FilterSettings` のペア。
2.  **ベンチマーク推奨プリセット (Benchmark Recommended Presets)**:
    *   `--benchmark` 完了後、算出された最適値をベースに以下のプリセットを自動生成し、適用を促すダイアログを表示する。
        *   **Standard (Office)**: 視認性と秘匿性のバランスを重視。静止ルーバー（0mm/s）。
        *   **High (Transit)**: 物理的な遮蔽力を最大化。
        *   **Stealth (Night)**: コントラスト崩壊を主軸にした低輝度環境向け。
    *   **マルチモニター対応**: 全ディスプレイを順次スキャンし、各画面の特性（PPI, パネルタイプ）に応じた固有のプリセット（例: "Office - LG UltraFine"）を自動生成する。
3.  **階層型ユーザーインターフェース (モニター主導型) [Phase 13]**:
    *   **モニター別設定**: 各モニターのサブメニュー内に、そのモニター用の「設定プリセット」メニューを集約。
        *   **適用 (Apply)**: 登録済みプリセットを一覧表示。現在の設定と一致するものにはチェックマークを表示する。
        *   **削除 (Delete)**: 不要なプリセットを個別に削除可能。
        *   **保存 (Save Current)**: 現在の設定を新規プリセットとして保存。
    *   **一括最適化**: トップレベルに「全画面を最適化する（一括ベンチマーク）」を配置。
4.  **メニューの部分更新 (Partial Update) [Phase 13]**:
    *   ベンチマーク実行中、進捗（%）を更新する際にメニュー全体を再構築せず、該当する `MenuItem` のラベルのみを `set_text` で書き換える。
    *   これにより、macOS 等でメニュー操作中にメニューが勝てに閉じる現象を回避し、UX を向上させる。
5.  **ベンチマーク結果ダイアログ**:
    *   完了後、各モニターの最適パラメータ（周期、遮蔽率）と、測定された Obfuscation Score の要約をダイアログ形式で表示する。
    *   マルチモニター環境では、全モニターの結果を一括で集計して表示する。
6.  **永続化**:
    *   `.config/softveil` 以下の `AppConfig` にシリアライズして保存され、アプリ再起動後も維持される。

### 4.12 AI監視 (Vigilance) モード (AI Vigilance Mode) [Phase 15]

カメラとローカルAIモデルを併用し、通常時は画面の視認性を100%維持しながら、覗き見リスクが生じた瞬間のみアトミックにフィルターを最大展開する高度防衛モード。

1.  **通常時 (Normal State)**:
    *   フィルター濃度 (Alpha) を `0`（完全透明）に設定し、正面からの画面の鮮明さや輝度・コントラストを一切阻害しない。
    *   バックグラウンドでカメラ入力を監視し、AI顔認識スレッドが覗き見（背後の複数人数）を継続的に監視する。
2.  **発動時 (Triggered State)**:
    *   覗き見（顔検出数 > 1）を検知すると、即座にフィルター形式を該当モニターの**パネル種別や画面カテゴリに応じたおすすめ設定**、またはユーザーが保存した**プリセット設定**へ自動的にマッピングして有効化する。
        *   OLED モニター: `HighIntensitySPD` (OLED 高密度ルーバー)
        *   LCD IPS モニター: OSのテーマに連動し、ライトモード時は `StealthLight`、ダークモード時は `StealthDark` を自動適用。
        *   プリセット優先: そのモニター用に保存されているプリセット（特に強力な `High`/`Transit` 等のプリセット）が存在する場合は、そのプリセットのフィルター形式および濃度（未設定時は 80%）を優先して動的適用する。
    *   濃度 (Alpha) を `80%` (204) またはプリセット濃度まで急上昇させ、斜め方向からの画面判読を物理的に不可能にする。
3.  **UI/UX 統合**:
    *   フィルター形式の選択肢に「AI 監視 (Vigilance)」を追加。
    *   トップレベルの肥大化を防ぐため、AI関連設定を「AI 覗き見検知 ▶ [現在のステータス]」という親メニューに集約。親メニューのラベルに現在のモード（`[オフ]`、`[✓ Vigilance]`、`[✓ 常時フィルター強化]`）を動的に表示し、サブメニューを開かずともワンクリック（一目）で有効状態を確認可能にした。
    *   サブメニュー内は排他選択（3択ラジオ形式：オフ / Vigilance（検知時のみ展開） / 常時フィルター強化）に統合し、現在のモードが一目で把握できるようにした。
    *   ユーザーがモニターの設定で「AI 監視 (Vigilance)」を選択するか、「Vigilance」モードを有効にすると、グローバル設定の「AI 覗き見検知」状態が自動的に `有効` になり、AI処理スレッドが自動的に立ち上がる親切設計。
    *   複数画面（マルチディスプレイ）環境においても、各モニターが独立して「AI監視」を登録でき、個別または一括で機能する。
4.  **省電力と最適化**:
    *   未検出（通常時）は描画更新を完全停止（`ControlFlow::Wait`）させることで、GPU負荷を `0%` に抑制し、ノートPCのバッテリー持ちを最大化する。)、および各種上書きパラメータ（周期、遮蔽率、スクロール速度）を統合したデータ構造。
    *   `Preset`: 名前（例: "Office", "Transit"）と `FilterSettings` のペア。
2.  **ベンチマーク推奨プリセット (Benchmark Recommended Presets)**:
    *   `--benchmark` 完了後、算出された最適値をベースに以下のプリセットを自動生成し、適用を促すダイアログを表示する。
        *   **Standard (Office)**: 視認性と秘匿性のバランスを重視。静止ルーバー（0mm/s）。
        *   **High (Transit)**: 物理的な遮蔽力を最大化。
        *   **Stealth (Night)**: コントラスト崩壊を主軸にした低輝度環境向け。
    *   **マルチモニター対応**: 全ディスプレイを順次スキャンし、各画面の特性（PPI, パネルタイプ）に応じた固有のプリセット（例: "Office - LG UltraFine"）を自動生成する。
3.  **階層型ユーザーインターフェース (モニター主導型) [Phase 13]**:
    *   **モニター別設定**: 各モニターのサブメニュー内に、そのモニター用の「設定プリセット」メニューを集約。
        *   **適用 (Apply)**: 登録済みプリセットを一覧表示。現在の設定と一致するものにはチェックマークを表示する。
        *   **削除 (Delete)**: 不要なプリセットを個別に削除可能。
        *   **保存 (Save Current)**: 現在の設定を新規プリセットとして保存。
    *   **一括最適化**: トップレベルに「全画面を最適化する（一括ベンチマーク）」を配置。
4.  **メニューの部分更新 (Partial Update) [Phase 13]**:
    *   ベンチマーク実行中、進捗（%）を更新する際にメニュー全体を再構築せず、該当する `MenuItem` のラベルのみを `set_text` で書き換える。
    *   これにより、macOS 等でメニュー操作中にメニューが勝手に閉じる現象を回避し、UX を向上させる。
5.  **ベンチマーク結果ダイアログ**:
    *   完了後、各モニターの最適パラメータ（周期、遮蔽率）と、測定された Obfuscation Score の要約をダイアログ形式で表示する。
    *   マルチモニター環境では、全モニターの結果を一括で集計して表示する。
6.  **永続化**:
    *   `.config/softveil` 以下の `AppConfig` にシリアライズして保存され、アプリ再起動後も維持される。

## 1. 目的・概要

### 1.1 背景と課題
(略)

### 1.2 プロダクトの概要
**Softveil** は、macOS / Windows の画面全体に半透明のデジタルフィルター（ルーバー模様）をソフトウェアで実現するデスクトップアプリケーションである。
- **SPD（Software Defined Privacy Display）** 技術により、ディスプレイの物理特性をハックした高度な秘匿性を実現する。
- **目標**: 画面を完全に見えなくすることではなく、斜め方向からの**「判読不能（Unreadable）」**状態をソフトウェア制御のみで達成する。

---
...
## 4. SPD（ソフトウェア定義型プライバシーディスプレイ）技術仕様

### 4.4 視覚的妨害アルゴリズム (shader.wgsl)
(略)

### 4.6 次世代アドバンスド SPD 技術（実験的）

物理パネルのサブピクセル構造や色偏移特性を利用した、より高度な妨害手法：

1.  **サブピクセル・レンダリング・ハック (Subpixel UHD Jamming)**:
    - ピクセル単位ではなく、R・G・B のサブピクセル単位で干渉パターンを生成。
    - 正面からはわずかな解像度低下として知覚されるが、斜め方向ではサブピクセル間の干渉がモアレとして激しく現れ、文字の輪郭を破壊する。
2.  **色収差・色ズレ干渉 (Chromatic Aberration Interference)**:
    - R と B のチャンネルを意図的に 1〜2px 左右にずらしたパターンを重畳。
    - 斜め視野角での物理的な色偏移（Color Shift）をソフトウェア側で増幅させ、文字の周囲を虹色のノイズで埋め尽くす。
3.  **ダイナミック・ガンマ・シフティング (Dynamic Gamma Shifting)**:
    - 画面キャプチャによるコンテンツ解析に基づき、ピクセルごとに微細なガンマ補正を逆位相で適用。
    - 「斜めから見た時にだけ背景色と文字色が同じ輝度（フラット）に見える」状態を作り出し、コントラストを強制的に消失させる。
4.  **セマンティック・クリアリティ・リダクション (Semantic Clarity Reduction)**:
    - 画面上の「文字領域」や「入力フォーム」を特定し、そこだけに重点的に強力な明瞭度低下（PPI低下）を適用。画像や動画領域はクリアに保つ。

### 4.7 統合型ステルス・スイッチ (Integrated Stealth Switch) [Phase 6]

ユーザーが「ステルス・ダーク」モードを選択した際、OS環境とフィルタアルゴリズムをアトミックに連動させ、秘匿性と視認性の極限の両立を図る。ThinkPad X13 等のミドルPPI (170 PPI前後) IPS液晶において、従来の「遮蔽」では不十分だった問題を「コントラスト破壊」によって解決する。

1.  **環境同期 (Environment Synchronization)**:
    *   OS の外観モード（ライト/ダーク）を強制的に「ダーク」へ変更。
    *   システム全体の輝度をあらかじめ設定された「ステルス輝度（例: 20-30%）」へ自動減衰。
    *   元の OS 設定（テーマ、輝度）はメモリにスナップショットとして保持し、モード解除時に自動復元する。
2.  **低輝度コントラスト崩壊 (Low-Luma Contrast Collapse / LLCC)**:
    *   [LCD 向け] 低輝度時の液晶特性（斜めからの黒浮き/IPS Glow）を利用。
    *   背景の黒をわずかに持ち上げる「ベースグロー」を重畳。正面からは黒に沈んで見えるが、斜めからは文字の輝度と背景が同化し、コントラストが強制消失する。
3.  **静的 UHF ディザリング (Static Ultra-High Frequency Dithering)**:
    *   [🔁 共通] 170 PPI 環境でのフリッカーを排除するため、動きのない固定ディザパターンを採用。
    *   物理ピクセル配列に最適化された 1x1 模様が、斜め方向の光に対してのみ微細な「拡散層」として機能し、文字の輪郭を破壊する。
4.  **超狭開口部強化 (Narrow Aperture Enhancement / UNA)**:
    *   [OLED 向け] 背景が「真の黒」であることを前提に、発光ピクセル（文字等）の開口部を極限まで絞る。
5.  **自動適応モード**:
    *   ユーザーが OS 側でダークモードを選択した場合も、自動的にこの最適化フィルタが適用される（独立したフィルタ形式としても選択可能）。

### 4.8 ハイブリッド SPD 制御 (Hybrid SPD Control) [Phase 6+]

ディスプレイの多様性（パネルの応答速度、サブピクセル配列、リフレッシュレート特性）に対応するため、自動推奨設定をベースとしつつ、最終的な視覚的品質をユーザーが最適化できる階層構造を導入する。

1.  **階層化パラメータ・スタック**:
    *   **Level 1: System Default**: プログラム定数。
    *   **Level 2: Heuristic Recommended (Current)**: 画面カテゴリ（PPI, scale_factor）とパネル種別、解像度から静的ルールに基づき自動計算。
        *   **Windows FHD (150-170 PPI)**: `period_mm = 0.50`, `scroll_speed = 0.0` (静止), `bidirectional = false` (縦縞) を推奨。
        *   **macOS Retina (220-250 PPI)**: `period_mm = 0.40`, `scroll_speed = 5.0`, `bidirectional = true` (格子) を推奨。
    *   **Level 3: Benchmark-Driven Auto-Optimization (Future)**: 実行環境でバックグラウンド・ベンチマーク（機械的評価）を行い、最も Obfuscation Index が高いパラメータを動的に選択する機能。
    *   **Level 4: Manual Override**: ユーザーによる「高度な微調整」メニューからの上書き設定。
2.  **微調整可能項目**:
    *   `period_mm`: 縞の太さ。LCDの干渉ノイズ（格子模様）を抑えるための主要な手段。
    *   `cover_ratio`: 遮蔽率。視野角ごとの秘匿強度と正面からの視認性のバランス。
    *   `scroll_speed`: スクロール速度。0mm/s（静止）に設定することで、低品質なLCDで発生しやすいフリッカーを完全に排除する。
3.  **アトミック・リカバリ**:
    *   「おすすめ設定」の適用により、すべての Manual Override を即座に破棄し、Heuristic Recommended に回帰できる設計。

### 4.9 ステルス・ライト・モード (Stealth Light Mode) [Phase 8]

ライトモード（背景が明るい環境）において、コントラストの動的破壊とサブピクセル構造のハックを組み合わせ、秘匿性と視認性を両立させる。Phase 14 において、より強力なサブピクセル方式へ一本化された。

1.  **サブピクセル・エッジ破壊 (Subpixel UHD Jamming)**:
    *   R・G・B のサブピクセル単位で異なる位相のパターンを重畳。物理的なサブピクセル構造を利用して、斜め方向からの文字の輪郭を物理的に破壊する。
2.  **高輝度コントラスト崩壊 (High-Luma Contrast Collapse / HLCC)**:
    *   背景（白）をわずかに沈めるベースベールを重畳。正面からは淡いグレーに見えるが、斜めからは液晶の視野角特性を突いて文字と背景を同化させる。

### 4.10 動的最適化と次世代評価指標 [Phase 10]

1.  **実機ベンチマークに基づく自動最適化 (Benchmark-Driven Auto-Optimization)**:
    *   初回起動時やキャリブレーション実行時に、複数のフィルタパラメータ（周期、遮蔽率）をミリ秒単位でテストし、その環境で最も Obfuscation Index が高くなる設定を自動選択する。
    *   **自動適用 [Phase 14]**: ベンチマーク完了後、最も秘匿性の高い「High (Transit)」プリセットを全対象モニターに自動適用し、即座に実戦投入可能な状態にする。
2.  **OCR 認識成功率による品質評価 (OCR Jamming Metrics)**:
    *   ベンチマークプロセスにおいて、Tesseract 等の OCR エンジンを呼び出し、実際の文字認識成功率を測定。これをプライバシー保護性能の最終的な KPI とする。 (🚧 開発中)


...

```toml
[dependencies]
# ── ウィンドウ管理・イベントループ ──────────────────────────────
tao = { version = "0.30", features = ["rwh_06"] }

# ── GPU レンダリング (Phase 5) ────────────────────────────────
wgpu       = "0.20"
bytemuck   = { version = "1.16", features = ["derive"] }

# ── 画面キャプチャ (Phase 5 実験的) ───────────────────────────
crabgrab   = { version = "0.4", features = ["wgpu", "bitmap"] }

# ── AI 覗き見検知 (Phase 3) ──────────────────────────────────
nokhwa     = { version = "0.10", features = ["input-native"] }
tract-onnx = "0.21"
```

---

## 3. 機能要件

### F-12　パネル適応型最適化 (Panel-Adaptive Optimization)

ディスプレイの物理特性（OLED / LCD IPS / LCD TN）を自動判別し、それぞれの弱点を補う最適なフィルタ戦略を適用する。

#### 1. フィルタ・アニメーション・プロファイル (SPD v2)

パネル種別に応じて、シェーダーによる高周波妨害を適用する。

| パネル | 判定基準 (macOS/Windows) | 最適化戦略 |
|:---|:---|:---|
| **OLED** | EDR > 2.0, モデル名 "OLED" | **高密度ルーバー (OledHighDensityLouver)** + バーンイン防止 (0.15px/s 偏移) |
| **LCD IPS** | リフレッシュレート, モデル名 | **高速位相反転** (30Hz) + 高速垂直スクロール (600px/s) |
| **LCD TN** | キーワードマッチ | **高速垂直スクロール** (300px/s) |
| **Unknown** | デフォルト | 安全側の 300px/s スクロール + 格子パターン |

---

## 4. SPD（ソフトウェア定義型プライバシーディスプレイ）技術仕様

### 4.4 視覚的妨害アルゴリズム (shader.wgsl)

1.  **時間軸干渉 (Temporal Interference / Kinetic Void)**: [OLED V4]
    - 30Hz 周期でルーバーの位相を反転。フリッカー融合により正面からは透明度を維持し、斜めからは残像を破壊して判読不能にする。
2.  **サブピクセル・ジャミング (Subpixel Jamming)**: [LCD V2]
    - R/G/B サブピクセル単位で異なる位相の超高密度パターンを重畳。物理配列との干渉により強烈な色モアレ（偽色）を発生させる。
3.  **高密度遮蔽 (Ultra-High Density Louver)**:
    - 遮蔽率 90% 以上、周期 2〜3px の極細スリットにより、斜め方向からの光を物理ピクセル構造レベルで遮断する。
4.  **適応型強度制御 (Intensity Mapping)**:
    - `intensity` 設定をスリット幅の倍率にマッピングし、物理的な秘匿強度を直感的に調整可能にする。

5.  **次世代 LCD 秘匿技術「NarrowMask」 (Narrow Aperture Masking)**: [Phase 12.2]
    *   **コンセプト**: 「縞を描く」のではなく「1px の開口部を残して残りを完全遮蔽 (alpha=1.0) する」。
    *   **原理**: 1px 窓 (gap) を残し、残りの stripe 部分を完全に黒で埋める。斜めから見た際に、1.0 のアルゴリズムが連続して視認されることで、幾何学的に急激な透過率の低下（≈90% 以上の遮蔽）を実現する。
    *   **パラメーター**:
        *   `gap_px = 1.0` (固定)
        *   `stripe_px = floor(1.0 / (1.0 - cover_ratio))`
        *   例: `cover_ratio = 0.75` のとき `stripe = 3px`, `gap = 1px` (遮蔽率 75%)。

### 4.5 描画アーキテクチャ

- **GPU パイプライン**: `wgpu` を使用したピクセルパーフェクトなシェーダー実行。
- **VSync 同期**: `PresentMode::Fifo` をデフォルトとし、ディスプレイのリフレッシュレートに同期した安定した描画を行う。
- **動的イベントループ**: 
    - アニメーション有効時は `ControlFlow::Poll` に切り替え、`MainEventsCleared` で描画を駆動する。
    - フィルター OFF または静止ディザ時は `ControlFlow::Wait` に移行し、不要な GPU 負荷を排除する。
    - 省電力 GPU 環境向けに、モードに応じてフレームレート上限（30/60fps）を適用する。
- **Alpha ブレンディング**: `discard` を排除し、すべてのピクセルで Alpha 値を持つ `vec4` を返すことで、OS レベルの透過合成と連動する。

---

## 7. プラットフォーム固有仕様

### 7.1 macOS 権限管理 (Phase 5/13)

- **画面収録権限**: 起動時の強制プロンプトを廃止し、永続的な `screen_capture_authorized` フラグによって次回以降のチェックをスキップする。
- **実測ベースの堅牢な判定 (Robust Check)**: `CGPreflightScreenCaptureAccess` の偽陰性対策として、`CGDisplayCreateImageForRect` を用いた 1x1 ピクセルの実キャプチャテストを行う。
- **非ブロック型オンデマンド警告**: 
    - ベンチマーク開始時に権限を確認するが、判定が失敗しても即座にエラーを表示せず実行を継続する。
    - ベンチマークプロセス内での実際のキャプチャ成否を最終的なエビデンスとし、成功した時点で権限フラグを自動更新する。これにより、OS 側の設定反映遅延による誤作動を完全に排除する。

