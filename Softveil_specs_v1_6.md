# 覗き見防止フィルターアプリ「Softveil」基本仕様書

**バージョン:** 2.4
**作成日:** 2026年5月17日
**ステータス:** Phase 6 統合型ステルス・スイッチ設計追加

**変更履歴:**

| バージョン | 変更内容 |
|-----------|---------|
| 1.0 - 2.0 | 初期開発〜Ver 2.0 拡張、AI 覗き見検知実装 |
| 2.1 - 2.3 | パネル適応型最適化、wgpu 移行、SPD 基盤、次世代 SPD 技術（実験的）統合 |
| 2.4 | **Phase 6 統合型ステルス・スイッチ**。OSテーマ連動、輝度操作、ダーク環境最適化フィルタの統合。 |

---

## 1. 目的・概要

### 1.1 背景と課題
(略)

### 1.2 プロダクトの概要
**Softveil** は、macOS / Windows の画面全体に半透明のデジタルフィルター（ルーバー模様）をソフトウェアで実現するデスクトップアプリケーションである。
- **SPD（Software Defined Privacy Display）** 技術により、ディスプレイの物理特性をハックした高度な秘匿性を実現する。
- **目標**: 画面を完全に見えなくすることではなく、斜め方向からの**「判読不能（Unreadable）」**状態をソフトウェア制御のみで達成する。

---
...
## 4. SPD（ソフトウェア定義型プライバシーディスプレイ）技術仕様

### 4.4 視覚的妨害アルゴリズム (shader.wgsl)
(略)

### 4.6 次世代アドバンスド SPD 技術（実験的）

物理パネルのサブピクセル構造や色偏移特性を利用した、より高度な妨害手法：

1.  **サブピクセル・レンダリング・ハック (Subpixel UHD Jamming)**:
    - ピクセル単位ではなく、R・G・B のサブピクセル単位で干渉パターンを生成。
    - 正面からはわずかな解像度低下として知覚されるが、斜め方向ではサブピクセル間の干渉がモアレとして激しく現れ、文字の輪郭を破壊する。
2.  **色収差・色ズレ干渉 (Chromatic Aberration Interference)**:
    - R と B のチャンネルを意図的に 1〜2px 左右にずらしたパターンを重畳。
    - 斜め視野角での物理的な色偏移（Color Shift）をソフトウェア側で増幅させ、文字の周囲を虹色のノイズで埋め尽くす。
3.  **ダイナミック・ガンマ・シフティング (Dynamic Gamma Shifting)**:
    - 画面キャプチャによるコンテンツ解析に基づき、ピクセルごとに微細なガンマ補正を逆位相で適用。
    - 「斜めから見た時にだけ背景色と文字色が同じ輝度（フラット）に見える」状態を作り出し、コントラストを強制的に消失させる。
4.  **セマンティック・クリアリティ・リダクション (Semantic Clarity Reduction)**:
    - 画面上の「文字領域」や「入力フォーム」を特定し、そこだけに重点的に強力な明瞭度低下（PPI低下）を適用。画像や動画領域はクリアに保つ。

### 4.7 統合型ステルス・スイッチ (Integrated Stealth Switch) [Phase 6]

ユーザーが「ステルス・ダーク」モードを選択した際、OS環境とフィルタアルゴリズムをアトミックに連動させ、秘匿性と視認性の極限の両立を図る。ThinkPad X13 等のミドルPPI (170 PPI前後) IPS液晶において、従来の「遮蔽」では不十分だった問題を「コントラスト破壊」によって解決する。

1.  **環境同期 (Environment Synchronization)**:
    *   OS の外観モード（ライト/ダーク）を強制的に「ダーク」へ変更。
    *   システム全体の輝度をあらかじめ設定された「ステルス輝度（例: 20-30%）」へ自動減衰。
    *   元の OS 設定（テーマ、輝度）はメモリにスナップショットとして保持し、モード解除時に自動復元する。
2.  **低輝度コントラスト崩壊 (Low-Luma Contrast Collapse / LLCC)**:
    *   [LCD 向け] 低輝度時の液晶特性（斜めからの黒浮き/IPS Glow）を利用。
    *   背景の黒をわずかに持ち上げる「ベースグロー」を重畳。正面からは黒に沈んで見えるが、斜めからは文字の輝度と背景が同化し、コントラストが強制消失する。
3.  **静的 UHF ディザリング (Static Ultra-High Frequency Dithering)**:
    *   [🔁 共通] 170 PPI 環境でのフリッカーを排除するため、動きのない固定ディザパターンを採用。
    *   物理ピクセル配列に最適化された 1x1 模様が、斜め方向の光に対してのみ微細な「拡散層」として機能し、文字の輪郭を破壊する。
4.  **超狭開口部強化 (Narrow Aperture Enhancement / UNA)**:
    *   [OLED 向け] 背景が「真の黒」であることを前提に、発光ピクセル（文字等）の開口部を極限まで絞る。
5.  **自動適応モード**:
    *   ユーザーが OS 側でダークモードを選択した場合も、自動的にこの最適化フィルタが適用される（独立したフィルタ形式としても選択可能）。

...

```toml
[dependencies]
# ── ウィンドウ管理・イベントループ ──────────────────────────────
tao = { version = "0.30", features = ["rwh_06"] }

# ── GPU レンダリング (Phase 5) ────────────────────────────────
wgpu       = "0.20"
bytemuck   = { version = "1.16", features = ["derive"] }

# ── 画面キャプチャ (Phase 5 実験的) ───────────────────────────
crabgrab   = { version = "0.4", features = ["wgpu", "bitmap"] }

# ── AI 覗き見検知 (Phase 3) ──────────────────────────────────
nokhwa     = { version = "0.10", features = ["input-native"] }
tract-onnx = "0.21"
```

---

## 3. 機能要件

### F-12　パネル適応型最適化 (Panel-Adaptive Optimization)

ディスプレイの物理特性（OLED / LCD IPS / LCD TN）を自動判別し、それぞれの弱点を補う最適なフィルタ戦略を適用する。

#### 1. フィルタ・アニメーション・プロファイル (SPD v2)

パネル種別に応じて、シェーダーによる高周波妨害を適用する。

| パネル | 判定基準 (macOS/Windows) | 最適化戦略 |
|:---|:---|:---|
| **OLED** | EDR > 2.0, モデル名 "OLED" | **高密度ルーバー (OledHighDensityLouver)** + バーンイン防止 (0.15px/s 偏移) |
| **LCD IPS** | リフレッシュレート, モデル名 | **高速位相反転** (30Hz) + 高速垂直スクロール (600px/s) |
| **LCD TN** | キーワードマッチ | **高速垂直スクロール** (300px/s) |
| **Unknown** | デフォルト | 安全側の 300px/s スクロール + 格子パターン |

---

## 4. SPD（ソフトウェア定義型プライバシーディスプレイ）技術仕様

### 4.4 視覚的妨害アルゴリズム (shader.wgsl)

1.  **時間軸干渉 (Temporal Interference / Kinetic Void)**: [OLED V4]
    - 30Hz 周期でルーバーの位相を反転。フリッカー融合により正面からは透明度を維持し、斜めからは残像を破壊して判読不能にする。
2.  **サブピクセル・ジャミング (Subpixel Jamming)**: [LCD V2]
    - R/G/B サブピクセル単位で異なる位相の超高密度パターンを重畳。物理配列との干渉により強烈な色モアレ（偽色）を発生させる。
3.  **高密度遮蔽 (Ultra-High Density Louver)**:
    - 遮蔽率 90% 以上、周期 2〜3px の極細スリットにより、斜め方向からの光を物理ピクセル構造レベルで遮断する。
4.  **適応型強度制御 (Intensity Mapping)**:
    - `intensity` 設定をスリット幅の倍率にマッピングし、物理的な秘匿強度を直感的に調整可能にする。

### 4.5 描画アーキテクチャ

- **GPU パイプライン**: `wgpu` を使用したピクセルパーフェクトなシェーダー実行。
- **動的イベントループ**: アニメーション有効時は `ControlFlow::Poll` に切り替え、`MainEventsCleared` で 60fps+ の描画を駆動する。
- **Alpha ブレンディング**: `discard` を排除し、すべてのピクセルで Alpha 値を持つ `vec4` を返すことで、OS レベルの透過合成と連動する。

---

## 7. プラットフォーム固有仕様

### 7.1 macOS 権限管理 (Phase 5)

- **画面収録権限**: 起動時の強制プロンプトを廃止。高度な解析が必要な機能（AI OCR 妨害等）が選択された際、権限が不足している場合にのみカスタムダイアログを表示する。
