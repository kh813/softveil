[English Version](MANUAL_en.md)

# Softveil ユーザーマニュアル (v0.1.23)

**Softveil** は macOS / Windows 向けの次世代ソフトウェア・プライバシーフィルターです。  
画面全体に特殊な干渉パターンを重ねることで、周囲からの覗き見（ビジュアル・ハッキング）を物理特性レベルで防止します。

---

## 🚀 クイックスタート (1分で開始)

1.  **起動**: `Softveil` を実行します。画面が薄いレイヤーで覆われれば動作しています。
2.  **基本操作**: メニューバー（macOS）または通知領域（Windows）のアイコンをクリックしてメニューを開きます。
3.  **最適化**: 初めて使うディスプレイでは、メニューから**「全画面を最適化する」**を実行してください。数分間のベンチマーク後、その画面に最適な最強設定（Transit）が自動適用されます。
4.  **ON/OFF**: `Cmd+Shift+P` (macOS) / `Ctrl+Shift+P` (Windows) でいつでもフィルターを消去/再表示できます。

---

## 目次

1. [インストールと初回設定](#1-インストールと初回設定)
2. [基本的な使い方とショートカット](#2-基本的な使い方とショートカット)
3. [メニュー項目の詳細説明](#3-メニュー項目の詳細説明)
4. [フィルター形式の選び方](#4-フィルター形式の選び方)
5. [AI 覗き見検知とプライバシー](#5-ai-覗き見検知とプライバシー)
6. [トラブルシューティングとFAQ](#6-トラブルシューティングとfaq)
7. [健康への配慮 (眼精疲労)](#7-健康への配慮-眼精疲労)
8. [サポートとフィードバック](#8-サポートとフィードバック)

---

## 1. インストールと初回設定

### ダウンロード

[リリースページ](https://github.com/yourname/softveil/releases) から対応するファイルをダウンロードしてください。

| プラットフォーム | ファイル | 備考 |
|:---|:---|:---|
| macOS | `Softveil-macOS.zip` | ユニバーサルバイナリ（Apple Silicon / Intel 両対応） |
| Windows | `Softveil-Windows.zip` | **スタンドアロン実行ファイル**（AIモデル内蔵） |

### 🍎 macOS の初回設定 (重要)

macOS では、セキュリティ保護のため以下の設定が必要です。

1.  **インストール**: ZIPを展開し、`Softveil.app` を `/Applications` (アプリケーション) フォルダに移動します。
2.  **起動**: アイコンをダブルクリックします。
3.  **権限の許可**:
    *   **アクセシビリティ**: フィルターを最前面に表示し、ショートカットを機能させるために必要です。OSのダイアログに従って「システム設定 > プライバシーとセキュリティ > アクセシビリティ」で Softveil を許可してください。
    *   **画面収録 (ベンチマーク時のみ)**: 「全画面を最適化する（ベンチマーク）」を実行する際に許可が求められます。これは画面の保護性能を客観的に測定するためにのみ使用されます。
    > **💡 改善**: 許可済みの場合、起動時に毎回確認ダイアログが出ることはありません。

### 🪟 Windows のインストール

1.  ダウンロードした ZIP を任意のフォルダ（例: `Documents\Softveil`）に展開します。
2.  `softveil.exe` を起動します。
    > **💡 ヒント**: AI モデル（ONNX）はバイナリに内蔵されているため、外部ファイルは不要です。

---

## 2. 基本的な使い方とショートカット

### 基本操作

*   **フィルターの消去/再表示**: トレイメニューの「フィルター：すべてオン」のチェックを切り替えるか、ショートカットを使用します。
*   **クリック透過**: フィルターが表示されていても、背後のウィンドウやボタンは通常通りクリック・操作できます。
*   **終了**: メニューの「Softveil を終了」を選択します。

### ショートカット

| 操作 | macOS | Windows |
|:---|:---|:---|
| フィルター 全体 ON/OFF | `Cmd + Shift + P` | `Ctrl + Shift + P` |

---

## 3. メニュー項目の詳細説明

メニューバー（macOS）またはシステムトレイ（Windows）のアイコンをクリックして表示されるメニューの構造と選択肢の一覧です。

### 📋 メニュー階層ツリー

```text
Softveil システムトレイメニュー
├── フィルター：すべてオン [☑️] (グローバル ON/OFF)
│
├── [接続モニター名] (例: 内蔵 Retina ディスプレイ) ─────────────────┐
│   ├── 画面タイプ: ノートPC 高解像度 (PPI: 220) [情報表示]          │
│   ├── フィルターを有効 [☑️]                                         │
│   ├─────────────────────────────────────────────────────────────┤
│   ├── フィルター形式  ▶                                           │
│   │   ├── SPD プロテクト ✦  (→ §4 参照)                            │
│   │   ├── ステルス・ダーク                                          │
│   │   ├── ステルス・ライト                                          │
│   │   ├── 標準ルーバー                                              │
│   │   ├── OCR妨害 (カメラ撮影対策)                                    │
│   │   └── 単色（輝度を抑える）                                      │
│   │
│   ├── フィルター濃度 (Alpha)  ▶ (0% 〜 100%)                       │
│   ├── フィルター強度 (Intensity)  ▶ (最高 / 高 / 標準 / 低 / 最低)    │
│   ├─────────────────────────────────────────────────────────────┤
│   ├── 設定プリセット  ▶                                           │
│   │   ├── プリセット適用  ▶ [保存プリセット一覧] (Standard / High等) │
│   │   ├── プリセット削除  ▶ [保存プリセット一覧]                    │
│   │   └── 現在の設定を保存...                                       │
│   │
│   ├── おすすめ設定に戻す                                           │
│   ├─────────────────────────────────────────────────────────────┤
│   ├── 高度な微調整  ▶                                             │
│   │   ├── 縞の太さ (Period)  ▶ (自動 / 細い / 標準 / 太い / 極太)  │
│   │   ├── 遮蔽率 (Cover Ratio)  ▶ (自動 / 低 / 標準 / 高 / 最高)    │
│   │   └── スクロール速度  ▶ (自動 / 静止 0mm/s / 極低速 / 低速 / 標準)│
│   │
│   ├── パネル種別を変更 ([現在の種別])  ▶                            │
│   │   └── Unknown (不明) / OLED (有機EL) / LCD IPS / LCD TN         │
│   │
│   └── 画面タイプを変更  ▶                                           │
│       └── ノートPC FHD / ノートPC 高解像度 / 外付け大型4K / 外付け標準│
│                                                                  └─┘
├── 最適化 (ベンチマーク)  ▶
│   ├── 全画面を最適化する                                            
│   └── [各モニター名] を個別で最適化 (マルチモニター時のみ表示)
├── AI 覗き見検知 ▶ [現在のステータス]
│   ├── オフ
│   ├── Vigilance（検知時のみ展開）
│   └── 常時フィルター強化
├── ログイン時に起動 [ ]
│
└── Softveil を終了
```

### 🔄 最適化 (ベンチマーク)
お手持ちのモニターの物理特性を自動解析し、最も覗き見を防止できる設定を生成・適用します。クイックスタート実行後、まず最初に行うべき最重要のセットアップ手順です。
*   **全画面を最適化する**: 接続されているすべてのディスプレイに対して一括でベンチマークを実行します。実行後、外出先での利用に最適な **「Transit (Maximum)」プリセットが全画面に自動適用** されます。
*   **[モニター名] を個別で最適化**: 複数モニター環境において、特定のモニターのみを個別で最適化したい場合に実行します。

### 🖥️ ディスプレイ別設定 (モニター名)
接続されているモニターごとに個別のフィルター特性、濃度、詳細微調整が可能です。
*   **画面タイプ / パネル種別 (情報表示)**: 現在のモニターが OLED（有機EL）か LCD（液晶）か、および PPI 値などの自動判定結果を表示します。
*   **フィルターを有効**: このディスプレイのフィルター表示を個別にオン/オフします。
*   **フィルター形式**: 描画アルゴリズムを選択します。詳細は [4. フィルター形式の選び方](#4-フィルター形式の選び方) を参照してください。
*   **フィルター濃度 (Alpha)**: フィルターの「濃さ（透明度）」を 0% 〜 100% の範囲で調整します。
*   **フィルター強度 (Intensity)**: 縞模様の「細かさ」を調整します。「最高」にすると縞が極細になり、正面からの視認違和感が減りますが、環境によってはフリッカー（ちらつき）を感じる場合があります。
*   **設定プリセット**: 保存した設定を呼び出したり、現在の設定を名前を付けて保存できます。
*   **おすすめ設定に戻す**: 手動で調整しすぎて分からなくなった場合、このボタンでディスプレイ判定に基づいた最適な初期状態に戻せます。
*   **高度な微調整 / パネル種別変更 / 画面タイプ変更 (詳細設定グループ)**:
    *   自動判定がうまくいかない場合や、特定の周波数で目が疲れる場合に「縞の太さ (Period)」「遮蔽率 (Cover Ratio)」「スクロール速度」を手動で上書きできます。また、パネル種別（OLED/LCD IPS等）や画面カテゴリーも手動で強制変更可能です。

### ⚙️ その他のグローバル設定項目
*   **フィルター：すべてオン**: すべてのディスプレイのフィルターを一括して表示/非表示にします。
*   **AI 覗き見検知 ▶ [現在のステータス] (排他選択サブメニュー)**:
    *   **現在のステータス表示**: 親メニューのラベルに現在アクティブなモード名（`[オフ]`、`[✓ Vigilance]`、`[✓ 常時フィルター強化]`）が動的に添えられ、クリックしなくても瞬時に有効ステータスを確認できます。
    *   **オフ**: AIによるカメラ覗き見検知機能およびカメラスレッドを完全に無効化（省電力）にします。
    *   **Vigilance（検知時のみ展開）**: 通常時は完全透明（フィルター濃度 0%）で正面からの視界を 100% 維持し、カメラが覗き見（背後の複数人数）を検知した瞬間のみ、アトミックにその画面の最適（またはプリセット）フィルターを展開します。詳細は [5. AI 覗き見検知とプライバシー](#5-ai-覗き見検知とプライバシー) を参照してください。
    *   **常時フィルター強化**: 通常時はユーザーが設定した通常のフィルター形式と濃度で描画し、背後に人が映ったときのみ、アトミックに濃度を 80% まで引き上げて物理的遮蔽力を最大化します。
*   **ログイン時に起動**: PCの起動・ログイン時に Softveil が自動的に起動するように LaunchAgent/レジストリ に登録します。
*   **Softveil を終了**: アプリを終了します。終了時、ステルスモードなどによって変更されていた OS のダークモードや画面輝度設定は自動的に元の状態に復元されます。

---

## 4. フィルター形式の選び方

Softveil は単純な「黒い膜」ではなく、ディスプレイの物理特性を突く複数のモードを搭載しています。

| モード | 推奨シーン | 特徴 |
|:---|:---|:---|
| **SPD プロテクト ✦** | **常用・移動中** | 最も遮蔽力が高い標準モード。パネルに合わせて自動最適化されます。（LCD では NarrowMask 技術を内包） |
| **ステルス・ダーク** | **夜間・暗い場所** | ダークモードに特化。OSも自動でダーク化し、背景と同化して文字を消します。 |
| **ステルス・ライト** | **昼間・明るい場所** | ライトモード向け。サブピクセル構造を突いて、斜めからの色偏移を増幅します。 |
| **OCR妨害 (カメラ撮影対策)** | **機密文書作成** | スマホカメラ等による撮影や AI 文字認識を困難にする特殊パターンを重畳します。 |
| **標準ルーバー** | **旧型PCなど** | 負荷が低い古典的な縦縞フィルター。 |

> **NarrowMask (LCD専用)**: LCD（液晶）モニターにおいて、1px の極細開口部以外を完全黒で塞ぐことで物理フィルターに近い遮蔽力を発揮します（SPDプロテクトに内包されており、Oled以外のLCDモニターで威力を発揮します）。

---

## 5. AI 覗き見検知とプライバシー

背後のカメラ画像を通じて周囲のビジュアルハッキングを自律防衛するためのAIモードです。

### 👁️ AI 監視モード — 覗き見検知時のみ展開
有効にすると、すべての画面で通常時は 0%（完全透明）に設定され、正面からの視認性を 100% 維持します。
カメラが背後の覗き見（複数人数）を検知した瞬間のみ、アトミックに最適なフィルターが展開されます。
*   **動的な最適フィルター適用**: 覗き見検知時、モニターのパネル種別やプリセットに基づいて自動的に最適な設定が選ばれます。
    *   OLED (有機EL): 強力な `SPD プロテクト ✦`（焼き付き防止・高密度ルーバー）を適用。
    *   LCD (液晶): OSテーマに応じて `ステルス・ライト` または `ステルス・ダーク` を自動的に切り替えて適用。
    *   プリセット優先: そのモニター用に保存されているプリセット（`High` または `Transit` など）があれば、それを優先して自動適用します。

### 👁️ AI 覗き見検知 — 常時フィルターを強化
このトグルが有効な場合、通常時はユーザーが指定した濃度でフィルターが機能し、カメラが背後の覗き見（複数人数）を検知した瞬間のみ、濃度 (Alpha) を即座に `80%` (204) まで引き上げて、斜め方向からの画面判読を物理的に不可能にします。

### 🛡️ プライバシーと安全性について
Softveil はユーザーのプライバシーを守るためのツールであり、アプリ自体が情報を収集することはありません。
*   **カメラ画像**: デバイス内で AI 解析された瞬間に破棄されます。保存や外部送信は一切行われません。
*   **画面キャプチャ**: ベンチマーク（最適化）時にのみ、メモリ内での性能測定に使用されます。ファイルとして保存されたり、外部へ送信されることはありません。

---

## 6. トラブルシューティングとFAQ

**Q: フィルターを表示すると画面がちらつく (フリッカー)**
A: モニターの更新タイミングと干渉している可能性があります。メニューの「高度な微調整 > スクロール速度」を「静止 (0mm/s)」に設定してみてください。

**Q: macOS で「全画面を最適化」が動作しない**
A: 「システム設定 > プライバシーとセキュリティ > 画面収録」で Softveil が許可されているか確認してください。最新版では、許可済みであればダイアログは表示されません。

**Q: Windows で一部のアプリの上にフィルターが出ない**
A: タスクマネージャーなどの管理者権限アプリや、一部のセキュリティ保護されたコンテンツの上には、OSの制限によりフィルターを重ねることができません。

---

## 7. 健康への配慮 (眼精疲労)

Softveil は視覚的なノイズを利用してプライバシーを守るため、目が疲れやすくなる場合があります。

*   **適度な休憩**: 1時間に一度はフィルターをオフにし、目を休めてください。
*   **環境光への適合**: 暗い場所では「ステルス・ダーク」、明るい場所では「ステルス・ライト」を使用することで、眼への刺激を抑えつつ秘匿性を維持できます。
*   **常用しない**: 周囲に人がいない安全な場所では、ショートカットでフィルターをオフにして使用することを推奨します。

---

## 8. サポートとフィードバック

不具合の報告や改善の提案は、GitHub の Issue または開発ログを通じてお寄せください。
物理的なプライバシーフィルターと Softveil を併用することで、最高レベルの保護環境を構築できます。
