# Softveil ユーザーマニュアル

**Softveil** は macOS / Windows 向けのソフトウェア・プライバシーフィルターです。  
画面全体に半透明のデジタルフィルターをかけ、カフェやオープンオフィスでの覗き見を防ぎます。

---

## 目次

1. [インストール](#1-インストール)
2. [基本的な使い方](#2-基本的な使い方)
3. [ショートカット](#3-ショートカット)
4. [トレイメニュー / メニューバー](#4-トレイメニュー--メニューバー)
5. [マルチディスプレイ](#5-マルチディスプレイ)
6. [トラブルシューティング](#6-トラブルシューティング)

---

## 1. インストール

### ダウンロード（推奨）

[リリースページ] から対応するファイルをダウンロードしてください。

| プラットフォーム | ファイル | 備考 |
|:---|:---|:---|
| macOS Apple Silicon | `Softveil.app` | ユニバーサルバイナリ（arm64 + x86_64） |
| macOS Intel | `Softveil.app` | 同上 |
| Windows x86-64 | `Softveil.exe` | 直接実行またはスタートアップに登録 |

**macOS のインストール:**
```
Softveil.app を /Applications フォルダにドラッグ＆ドロップ
```

> **macOS Gatekeeper**: 初回起動時にブロックされる場合があります。右クリック → 「開く」を選択するか、以下を実行してください:
> ```bash
> xattr -d com.apple.quarantine /Applications/Softveil.app
> ```

**Windows のインストール:**
```
Softveil.exe をダウンロードして任意のフォルダに配置し、ダブルクリックで起動。
スタートアップへの登録は「6. トラブルシューティング」を参照。
```

### ソースからビルド

**必要なもの**: Rust ツールチェーン (`rustup`)、macOS の場合は Xcode Command Line Tools

```bash
git clone https://github.com/yourname/softveil.git
cd softveil

# macOS ユニバーサルバイナリ
make

# Windows（GitHub Actions で自動ビルド）
# → リリースタグをプッシュすると自動的に Softveil.exe が生成されます
```

| make ターゲット | 説明 |
|:---|:---|
| `make` | 現在のホスト向けにビルド |
| `make all` | macOS ユニバーサルバイナリを生成 |
| `make clean` | ビルド成果物を削除 |

---

## 2. 基本的な使い方

### 起動

- **macOS**: Softveil.app をダブルクリック → メニューバーにアイコンが表示されます
- **Windows**: Softveil.exe をダブルクリック → タスクトレイにアイコンが表示されます

起動直後にフィルターが有効になり、画面全体が薄い半透明の黒レイヤーで覆われます。

### フィルターの ON / OFF

以下のいずれかの方法で切り替えられます:

| 方法 | 操作 |
|:---|:---|
| ショートカット | `Cmd+Shift+P`（macOS）/ `Ctrl+Shift+P`（Windows） |
| メニューバー（macOS） | アイコンをクリック → 「フィルター：すべてオン/オフ」 |
| システムトレイ（Windows） | アイコンを右クリック → 「フィルター：すべてオン/オフ」 |

### 終了

- **macOS**: メニューバーアイコン → 「Softveil を終了」
- **Windows**: トレイアイコンを右クリック → 「Softveil を終了」

> アプリはウィンドウを持たないバックグラウンドアプリです。Dock（macOS）やタスクバー（Windows）には表示されません。

---

## 3. ショートカット

| 操作 | macOS | Windows |
|:---|:---|:---|
| フィルター グローバル ON/OFF | `Cmd+Shift+P` | `Ctrl+Shift+P` |

フィルター越しに背後のアプリを通常どおり操作できます（クリック透過）。ショートカットは他のアプリがアクティブな状態でも動作します（グローバルショートカット）。

---

## 4. トレイメニュー / メニューバー

### メニュー構成

**全ディスプレイONの状態:**

```
[✓] フィルター：すべてオン
─────────────────────────────
ディスプレイ設定 ▶
  [✓] Built-in Display (2560×1600)
  [✓] DELL U2723D (2560×1440)
─────────────────────────────
Softveil を終了
```

**ノートPC画面だけOFFにした状態:**

```
[ ] フィルター：すべてオン
─────────────────────────────
ディスプレイ設定 ▶
  [ ] Built-in Display (2560×1600)   ← ノートPC画面: OFF
  [✓] DELL U2723D (2560×1440)        ← 外付けモニター: ON
─────────────────────────────
[✓] AI 覗き見検知
─────────────────────────────
Softveil を終了
```

### 各メニュー項目の説明

**「フィルター：すべてオン」（グローバルスイッチ）**

全ディスプレイのフィルターを一括で ON/OFF します。

- チェックマーク（✓）が付くのは、**全ディスプレイがON**のときだけです。1つでもOFFのディスプレイがあると外れます
- グローバルをOFFにしても、個別の設定は記憶されています。グローバルをONに戻すと、直前の個別設定がそのまま復活します

**「ディスプレイ設定」サブメニュー**

ディスプレイごとに個別に ON/OFF を切り替えます。「ノートPC画面は自分だけ見えればいい、外付けモニターは周囲から見える位置にある」といった場面で使います。

**「AI 覗き見検知」**

内蔵カメラを使用して背後の人物を自動検知し、覗き見されている間だけフィルターを自動的に濃くします。

- チェックを入れると検知が有効になります。初回起動時にカメラへのアクセス許可を求められます
- 検知された画像は外部に送信されたり保存されたりすることはありません。すべてお使いのPC内で安全に処理されます

**「Softveil を終了」**

アプリを完全終了します。次回はダブルクリックで再起動してください。

### グローバルと個別の優先関係

グローバルスイッチと個別設定は2段階になっています:

```
実際に表示 = グローバルON  かつ  そのディスプレイもON
```

| グローバル | 個別 | 表示 |
|:---:|:---:|:---:|
| ON | ON | ✅ フィルターあり |
| ON | OFF | ❌ フィルターなし |
| OFF | ON | ❌ フィルターなし |
| OFF | OFF | ❌ フィルターなし |

「会議中だけ全部消したい」はグローバルOFF、「ノートPC画面は常に素通し」は個別OFFが適しています。

---

## 5. マルチディスプレイ

Softveil は接続されているすべてのディスプレイを自動的に検出し、それぞれに独立したフィルターを表示します。

### ディスプレイごとの ON/OFF

起動直後は全ディスプレイのフィルターがONになります。特定のディスプレイだけOFFにするには「ディスプレイ設定」サブメニューから操作してください。

**典型的な使い方の例:**

| 場面 | ノートPC画面 | 外付けモニター |
|:---|:---:|:---:|
| カフェで作業（外付けモニターが周囲から見える） | OFF | ON |
| 来客対応中（プレゼン画面を見せたい） | OFF | OFF |
| 移動中（ノートPC1枚、周囲が近い） | ON | ― |

### ホットプラグ（接続・切断）

- **ディスプレイを接続**すると、自動的に新しいフィルターが追加されます（デフォルトON）
- **ディスプレイを切断**すると、そのフィルターは自動的に消えます
- **同じディスプレイを再接続**した場合、切断前の ON/OFF 設定が自動的に復元されます（位置と解像度が一致する場合）

### ディスプレイ名について

サブメニューに表示されるディスプレイ名（例: "Built-in Display"、"DELL U2723D"）はOSから取得した名前です。取得できない場合は "Display 1"、"Display 2" のように番号で表示されます。

---

## 6. トラブルシューティング

### macOS でアイコンが表示されない

「システム設定 → プライバシーとセキュリティ → アクセシビリティ」で Softveil を許可してください。グローバルショートカットの動作にも必要です。

### フィルターがフルスクリーンアプリの上に表示されない

macOS のフルスクリーンアプリでは表示できない場合があります（既知の制限）。フルスクリーンを一度解除してから Softveil を有効にしてください。

### ショートカットが反応しない（macOS）

初回起動時にアクセシビリティ権限のダイアログが表示されます。「システム設定」で Softveil に権限を付与してから再起動してください。

### Windows でフィルターが管理者権限ウィンドウの上に表示されない

これは Windows の UAC セキュリティ制限によるものです。管理者権限ウィンドウへの前面表示は現時点では対応していません。

### ログイン時に自動起動させたい

**macOS**: システム設定 → 一般 → ログイン項目 で Softveil.app を追加します。

**Windows**: `Win+R` → `shell:startup` を開き、`Softveil.exe` のショートカットを配置します。
