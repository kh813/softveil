# Softveil ユーザーマニュアル

**Softveil** は macOS / Windows 向けのソフトウェア・プライバシーフィルターです。  
画面全体に半透明のデジタルフィルターをかけ、カフェやオープンオフィスでの覗き見を防ぎます。

---

## 目次

1. [インストール](#1-インストール)
2. [基本的な使い方](#2-基本的な使い方)
3. [ショートカット](#3-ショートカット)
4. [トレイメニュー / メニューバー](#4-トレイメニュー--メニューバー)
5. [マルチディスプレイ](#5-マルチディスプレイ)
6. [トラブルシューティング](#6-トラブルシューティング)

---

## 1. インストール

### ダウンロード（推奨）

[リリースページ](https://github.com/yourname/softveil/releases) から対応するファイルをダウンロードしてください。

| プラットフォーム | ファイル | 備考 |
|:---|:---|:---|
| macOS | `Softveil-macOS.zip` | ユニバーサルバイナリ（Apple Silicon / Intel 両対応） |
| Windows | `Softveil-Windows.zip` | x64 用実行ファイルと AI モデルのセット |

**macOS のインストール:**
1. ダウンロードした ZIP を展開します。
2. `Softveil.app` を `/Applications` フォルダにドラッグ＆ドロップします。

> **macOS Gatekeeper**: 初回起動時に「開発元を確認できないため開けません」と表示される場合があります。その場合は `Softveil.app` を右クリックして「開く」を選択してください。

**Windows のインストール:**
1. ダウンロードした ZIP を任意のフォルダ（例: `C:\Users\YourName\AppData\Local\Softveil`）に展開します。
2. `softveil.exe` をダブルクリックして起動します。
3. `face_detector.onnx` ファイルは `softveil.exe` と同じ場所に置いたままにしてください。

### ソースからビルド

**必要なもの**: Rust ツールチェーン (`rustup`)、macOS の場合は Xcode Command Line Tools

```bash
git clone https://github.com/yourname/softveil.git
cd softveil

# macOS ユニバーサルバイナリの生成
make mac

# Windows（開発環境でのビルド）
cargo build --release
```

### CI/CD によるクロスコンパイル

GitHub Actions を利用して、任意のタイミングで最新コードのバイナリ（.app / .exe）を生成できます。

**1. タグを打ってリリース（推奨）**
新しいバージョンタグをプッシュすると、自動的にビルドとリリース作成が行われます。
```bash
git tag v0.1.x
git push origin v0.1.x
```

**2. 手動でビルドのみ実行**
GitHub CLI (`gh`) を使用して、現在のブランチの状態をクロスコンパイルします。
```bash
gh workflow run release.yml
```

**3. CI のスキップ**
テスト段階のコードをプッシュする際など、ビルドを走らせたくない場合はコミットメッセージに `[skip ci]` を含めてください。

---

## 2. 基本的な使い方

### 起動

- **macOS**: Softveil.app を起動すると、メニューバーにアイコン（グレーのシールド）が表示されます。
- **Windows**: softveil.exe を起動すると、タスクトレイにアイコンが表示されます。

起動直後にフィルターが有効になり、画面全体が薄い半透明の黒レイヤーで覆われます。

### フィルターの ON / OFF

以下のいずれかの方法で切り替えられます:

| 方法 | 操作 |
|:---|:---|
| ショートカット | `Cmd+Shift+P`（macOS）/ `Ctrl+Shift+P`（Windows） |
| メニューバー（macOS） | アイコンをクリック → 「フィルター：すべてオン」のチェックを外す |
| システムトレイ（Windows） | アイコンを右クリック → 「フィルター：すべてオン」のチェックを外す |

### 終了

- **macOS**: メニューバーアイコン → 「Softveil を終了」
- **Windows**: トレイアイコンを右クリック → 「Softveil を終了」

---

## 3. ショートカット

| 操作 | macOS | Windows |
|:---|:---|:---|
| フィルター 全体 ON/OFF | `Cmd+Shift+P` | `Ctrl+Shift+P` |

フィルター越しに背後のアプリを通常どおり操作できます（クリック透過）。ショートカットは他のアプリがアクティブな状態でも動作します。

---

## 4. トレイメニュー / メニューバー

### 各メニュー項目の説明

**「フィルター：すべてオン」**
全ディスプレイのフィルターを一括で ON/OFF します。

**「ディスプレイ別設定」メニュー（トップレベルに一覧表示）**
接続されている各ディスプレイの名称がメニューに表示されます。
- macOS では、`system_profiler` を用いて取得したメーカー名や型番（例: "BenQ EW2880U"）が表示されます。
- Windows では、レジストリから取得した EDID 情報を基に正確なモデル名が表示されます。
各ディスプレイのサブメニューから以下の個別設定が可能です。
- **画面タイプ**: 自動判定されたディスプレイの種類（ノートPC FHD / 高解像度 / 外付け等）が表示されます。手動で変更することも可能です。
- **フィルターを有効**: そのディスプレイのフィルター ON/OFF を切り替えます。
- **パネル種別**: 自動判定されたパネルの種類（OLED / LCD IPS / LCD TN）が表示されます。手動で変更することも可能です。
- **フィルター形式**: そのディスプレイに適用するパターン（単色 / 縦縞 / アニメーション / コントラスト妨害）を選択します。
    - **LCD 視野角コントラスト妨害**: LCD (IPS) ディスプレイ専用モード。画面全体を黒くする代わりに、LCD パネルの斜め方向でのコントラスト低下特性を利用した微細パターンを重畳します。正面からは画面がほとんど暗くなりませんが、斜め 45 度以上の視点では文字が判読困難になります。
- **フィルター強度**: パターンの密度や振動の大きさを調整します。MacBook Pro のような高精細ディスプレイで効果が薄い場合は「高」や「最高」に設定してください。
- **フィルター濃度**: そのディスプレイの不透明度（10%〜90%）を選択します。

**「AI 覗き見検知」**
内蔵カメラを使用して背後の人物を自動検知し、覗き見されている間だけフィルターを自動的に濃く（80%）します。
- 検知された画像は外部に送信されたり保存されたりすることはありません。すべてローカルで処理されます。

**「ログイン時に自動起動」**
チェックを入れると、PCの起動時に Softveil が自動的に開始されます。

### OLED 高密度ルーバー (Kinetic Void) について

OLED ディスプレイ（MacBook Pro 等）向けの最強強度プライバシーモードです。
超高密度な縦縞（遮蔽率 94%）を **30Hz で高速反転**させる「時間軸干渉」を採用しています。

- **正面からの見え方:** 人間の目の残像効果（フリッカー融合）により、画面がやや暗いグレーの層に覆われているように見えます。
- **斜めからの見え方:** OLED の高速な応答速度が時間的な揺らぎを強調し、激しいシマシマのノイズが発生して静止した文字の判読を困難にします。
- **Samsung Galaxy S26 Ultra との違い:** Samsung は物理的なルーバーで光を遮断しますが、Softveil V4 は物理構造と時間的な錯覚を組み合わせることで、ソフトウェアのみで同等の「読ませない」効果を追求しています。

### LCD 視野角コントラスト妨害 (Subpixel Jammer) について

LCD (IPS) ディスプレイ専用の強化モードです。
液晶のサブピクセル配列（RGB）をターゲットにした「サブピクセル・ジャミング」を搭載しています。

- **仕組み:** R・G・B 各チャンネルの位相をずらした 1px 単位の超微細パターンを重畳します。斜めから見た際に液晶の配向角と干渉し、強烈な虹色のモアレ（偽色）を発生させて情報の輪郭を破壊します。
- **併用効果:** 高速スクロールと輝度圧縮グリッドを組み合わせ、コントラストが低下しやすい LCD の弱点を突いて秘匿性を最大化します。

---

## 5. マルチディスプレイとホットプラグ

Softveil はディスプレイの接続・切断を自動的に検知します。

- **接続時**: 新しく接続されたディスプレイに自動的にフィルターを適用し、メニューのディスプレイ一覧を更新します。
- **再接続時**: 以前そのディスプレイで設定していた状態が自動的に復元されます。

---

## 6. トラブルシューティング

### macOS でショートカットが効かない / フィルターが表示されない
- 「システム設定 → プライバシーとセキュリティ → アクセシビリティ」で Softveil を許可してください。
- **画面収録の許可**: Phase 5 以降の高度な画面解析機能（キャプチャに基づくブラー等）のために、「システム設定 → プライバシーとセキュリティ → 画面収録」での許可が求められます。許可しない場合、一部の高度なフィルタ機能が動作しません。

### Windows で一部のウィンドウの上にフィルターが表示されない
タスクマネージャーや管理者権限で実行されている一部のアプリの上には、セキュリティ制限によりフィルターが表示されない場合があります。

### カメラが認識されない（AI 覗き見検知）
- クラムシェルモード（ノートPCを閉じている状態）ではカメラが利用できないため、この機能は動作しません。
- 他のアプリがカメラを占有している場合、検知が開始できないことがあります。

### 斜め方向の保護をさらに強化したい
Softveil はソフトウェアによる視野角制限のため、物理プライバシースクリーンフィルターほど斜め方向を完全に遮断することはできません。より強力な保護が必要な場面では、市販の物理プライバシーフィルターと Softveil を併用することを推奨します。

- **物理フィルター**: 出射角を物理的に制限し、斜め視野を根本から遮断します。
- **Softveil**: パターンによる追加難読化、AI 覗き見検知、および瞬時の ON/OFF 切り替えを提供します。

両者を併用することで、互いの弱点を補完し、最高レベルのプライバシー保護を実現できます。

---

## 7. 今後のアップデート予定

Softveil は現在、以下の次世代プライバシー保護技術の研究開発（Phase 5）を進めています。

- **光学ハック (SPD)**: 液晶ディスプレイの物理特性や人間の視覚特性（フリッカー融合）を利用した、次世代の秘匿技術を導入予定です。
    - **高速移動パターン**: 正面からは透明に見え、斜めからはノイズに見える高速振動フィルター。
    - **非対称曲線パターン**: 特定の視野角からのみ情報を破壊するシームレスパターン。
    - **サブピクセル干渉**: R/G/B サブピクセル単位でのレンダリングにより、斜め方向でのモアレと色偏移を劇的に増幅させ、文字を判読不能にします。
    - **色収差シミュレーション**: 意図的な色ズレを発生させ、斜め視野角での虹色ノイズによる遮蔽を強化します。
- **モバイル/外部ディスプレイ最適化**: 外出先のバッテリー利用時と、オフィスでの外部ディスプレイ利用時で、秘匿強度と消費電力を自動的に切り替えます。
- **AI OCR 妨害**: スマートフォン等による画面の撮影と AI による文字認識を困難にする特殊なテクスチャを重畳します。
- **ディスプレイ最適化**: モニターが LCD (液晶) か OLED (有機EL) かを自動判別し、焼き付き防止や視野角特性に合わせた最適な保護パターンを適用します。
- **ステルス・ダークモード (Phase 6)**: フィルターを「ステルス・ダーク」に切り替えるだけで、OSテーマのダーク化・輝度ダウン・専用フィルタ適用を同時に行います。

---

## 8. ステルス・ダークモード (Phase 6)

「画面を暗くしながら、自分だけは文字をはっきり読みたい」というニーズに応える高度な最適化モードです。

### 仕組みとメリット
- **自動環境同期**: フィルター形式から「ステルス・ダーク」を選択するだけで、PC全体が自動的にダークモードへ切り替わり、輝度が適切なレベルまで低下します。
- **正面視認性の特化**: 
    - **OLED**: 針の穴のような極細の開口部から光を通すことで、正面のユーザーにはシャープな文字を届け、斜めからは漆黒のモニターに見えるようにします。
    - **LCD**: 低輝度時の液晶特性（黒浮き）を突き、正面では背景に隠れているノイズが斜めからの視線をブロックします。
- **復元機能**: フィルターをオフにするか、他の形式に変更すれば、OSのテーマと輝度は自動的に元の状態に戻ります。
