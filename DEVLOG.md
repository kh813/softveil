# Softveil Devlog

開発中に発生した問題・決定・気づきを時系列で記録する。  
**運用ルール**:
- 実装セッション終了時に追記する
- 「何を試みたか」「なぜそうしたか」「何がうまくいかなかったか」を残す
- 解決した既知の問題は仕様書 §9 の Known Issues テーブルも ✅ に更新する

---

## テンプレート（コピーして使う）

```
## YYYY-MM-DD

### <フェーズ名> (<対象 OS/範囲>)
- **<対応内容の見出し>**: <詳細>
- **<対応内容の見出し>**: <詳細>

### 既知の問題 / 持ち越し
- <項目>
```

---

<!-- 以下に実装ログを追記していく -->

## 2026-05-21

### Phase 8: サブピクセル・レンダリング・ハックの実装 (Subpixel UHD Jamming)
- **Subpixel 制御の導入**:
    - `shader.wgsl` において `(x * 3.0)` 座標系を導入し、R/G/B サブピクセル単位での独立した透過制御を実現。
    - **Stealth Light (Subpixel) モード**: 0.5px 相当の極細ラインをサブピクセルごとに 1/3px ずつずらして配置するアルゴリズムを実装。文字エッジの MTF (Modulation Transfer Function) を物理的に破壊し、斜めからの色偏移（Chromatic Aberration）を意図的に増幅させることに成功。
    - **UI 統合**: トレイメニューに「Stealth Light (Subpixel)」を追加。

### Phase 10: 動的最適化 (Benchmark-Driven Auto-Optimization)
- **パラメータ自動選定ロジック**:
    - `--benchmark` モードにおいて、`period_mm` と `cover_ratio` の複数の組み合わせを自動テストし、Obfuscation Index が最大となる「ハードウェア固有の最適値」を算出する検索アルゴリズム `find_optimal_params` を実装。
    - **評価指標の拡充**: 新規追加したサブピクセルフィルタをベンチマーク対象に追加し、客観的な秘匿性能を測定。

### Windows Subsystem の調整
- **コンソールウィンドウの強制非表示**: `src/main.rs` の `windows_subsystem = "windows"` 属性を無条件に適用するよう修正（Debugビルドでも `cmd.exe` が開かないように変更）。
- **不要な標準出力の整理**: GUIモード起動時の不要な `println!` を整理。

### 既知の問題 / 持ち越し
- **OCR 認識率の統合**: Tesseract 環境の構築と、実際の文字認識率との相関分析。
- **動的パラメータの永続化**: 最適化されたパラメータを `DisplayConfig` に自動保存し、次回のフィルター適用時に反映させる仕組みの検討。

### Phase 8: ステルス・ライト & サブピクセル・ハック (Stealth Light Implementation)
- **StealthLight (HLCC) の実装**:
    - ライトモード向けのコントラスト崩壊アルゴリズムを `shader.wgsl` に実装。背景（白）をわずかに沈める `veil`（ベースベール）と 0.5px ラインによるエッジ破壊を組み合わせ。
    - **OS 同期**: フィルター選択時に OS テーマをライトモードへ、輝度を 20% へ自動調整するよう `AppState` を拡張。
    - **パラメータ・チューニング**: 初例での効果不足を改善するため、`veil` 強度を 0.32 へ強化後、視認性とのバランスを考慮し 0.22 に最終調整。

### Phase 9: 機械的ベンチマークの実装と最適化 (Mechanical Benchmarking)
- **ベンチマークモードの導入**: `--benchmark` 引数による自動評価プロセスを構築。
- **高速化と安定化の施策**:
    - **ネイティブ GDI キャプチャ**: Windows において PowerShell 経由のキャプチャを廃止し、Win32 GDI を用いた Rust ネイティブ実装へ移行。外部プロセス起動のハングアップを解消。
    - **軽量化ベンチマーク**: デバッグビルドでの極端な低速化を回避するため、解析前に画像を 1/2 に縮小し、保存形式を PNG から JPEG へ変更。
    - **メッセージポンプ**: Windows のメッセージループをベンチマーク中に明示的に回すことで、OS との通信を維持しフリーズを防止。
    - **メモリ内解析**: キャプチャした画像をファイルに書き出す前にメモリ上で直接 Obfuscation スコアを計算するフローへ最適化。

### 既知の問題 / 持ち越し
- **OCR 認識率の統合**: Tesseract 等を用いた実際の文字認識率の測定（Phase 10予定）。
- **サブピクセル制御の深化**: 色収差シミュレーション等の高度な物理ハックの追加検討。

### Phase 7: Windows 最適化とリファクタリング (Phase 6+ Improvement)
- **ネイティブ API への移行 (osascript/PowerShell 廃止)**:
    - **Windows WMI 直接制御**: 輝度取得・設定において PowerShell 呼び出しを完全に廃止し、`windows` クレートを用いた WMI/COM 直接アクセスへ移行。実行速度の向上とセキュリティリスク（スクリプトインジェクション）の排除。
    - **Windows ブロードキャスト実装**: レジストリによるテーマ変更後、`SendMessageTimeout` を用いて `WM_SETTINGCHANGE` をブロードキャストするよう実装。タスクバー等の他アプリへ変更が即時反映されるようになった。
    - **macOS Native API 統合**: `osascript` コマンドを廃止。輝度制御には `IOKit` (`IODisplaySetFloatParameter`)、アラート表示には `NSAlert`、ダークモード判定には `NSUserDefaults` を使用。設定変更には `NSAppleScript` をインメモリ実行することで、プロセス起動オーバーヘッドを削減。
- **シェーダー演算の最適化**:
    - **CPU 事前計算**: `VerticalLouver` モードの斜め成分計算で使用していた `cos`/`sin` 演算を CPU 側（Rust）に移動。毎フレーム、各ピクセルで実行されていた重い trig 関数を uniforms 経由の定数参照に置き換え、GPU 負荷をさらに低減。
- **Windows 視覚的品質の改善**:
    - **FHD ノート最適化**: Windows FHD (150-170 PPI) 向けに `period_mm` を 0.20mm に引き下げ。格子の目を微細化することで、スクリーンセーバーのような「格子感」を排除し、均一なベールのような視覚効果を達成。
    - **DPI Awareness 統合**: `SetProcessDpiAwarenessContext` を呼び出し、Per-Monitor DPI Aware v2 を有効化。これにより OS による強制スケーリングを回避し、物理ピクセルに基づいた正確なフィルタ描画を実現。
    - **格子状パターンの制御**: `bidirectional` フラグを導入。低 PPI 環境では視認されやすい千鳥格子を避け、デフォルトで縦縞（Vertical-only）を適用するよう修正。
- **パフォーマンスとシステム負荷の最適化**:
    - **フレームリミッター**: イベントループに 60fps 上限のフレームリミッターを実装。無制限なループによる GPU 負荷を抑制。
    - **動的描画停止**: 静止ディザ（スクロール速度 0）やフィルター OFF 時には `ControlFlow::Wait` に切り替え、GPU レンダリングを完全に停止するロジックを実装。
    - **Windows GPU バックエンド**: バックエンドを DX12/DX11 に優先順位付け。Intel 内蔵 GPU 等での安定性と効率を向上。
- **システム統合と堅牢性の向上**:
    - **PowerShell ウィンドウの隠蔽**: 輝度操作等の PowerShell 呼び出し時に `CREATE_NO_WINDOW` フラグと `-WindowStyle Hidden` を併用し、黒いコンソール画面が瞬間表示される問題を解決。
    - **アライメント検証**: `Uniforms` 構造体のサイズ (80bytes) とアライメント (16-byte) をコンパイル時に検証するアサーションを追加。
    - **設定の下位互換性**: `DisplayConfig` の全フィールドに `#[serde(default)]` を付与。将来のフィールド追加時にも既存の設定ファイルが読み込めるよう耐性を強化。

### 既知の問題 / 持ち越し
- **内蔵ディスプレイ判定の更なる強化**: `SetupAPI` を用いた ACPI バス接続判定への移行を検討。
- **crabgrab の遅延初期化**: 将来の Semantic Privacy 実装に向け、依存関係のロードタイミングを最適化予定。


## 2026-05-18

### Phase 6: 統合型ステルス・スイッチの実装 (v0.1.16)
- **OS 設定制御ブリッジ**: フィルター形式「ステルス・ダーク」の選択に連動し、OS のダークモード切り替えと輝度自動調整（25%）を行うロジックを実装。OS の元の設定は `OSSettingsSnapshot` としてメモリに保持し、モード解除時またはアプリ終了時に自動復元する。
- **ステルス専用フィルター (shader.wgsl)**: 
    - **LLCC (Low-Luma Contrast Collapse)**: LCD 向けに、わずかなグレー（ベースグロー）を重畳して黒浮きを意図的に発生させ、斜め方向からのコントラストを消失させる新アルゴリズム。
    - **UNA (Narrow Aperture Enhancement)**: OLED 向けに、物理ピクセル配列に最適化した超狭開口部パターンを適用。
    - **Static UHF Dithering**: 1x1 の固定ディザパターンにより、動きを排除しつつ高周波な遮蔽を実現。
- **プラットフォーム固有の実装**: 
    - macOS: `osascript` を利用したシステム外観（Dark Mode）と輝度キーコード送信による制御。
    - Windows: レジストリ (`AppsUseLightTheme`) によるテーマ制御と、PowerShell (WMI) を介した輝度操作を統合。
- **UI 統合**: トレイメニューに「ステルス・ダーク (LLCC)」を追加。

### 既知の問題 / 持ち越し
- **macOS 輝度制御の精度**: 現在はキーコード送信による擬似的な制御のため、環境によっては 25% への正確な収束が難しい場合がある。将来的に IOKit による直接制御を検討。
- **Windows PowerShell のオーバーヘッド**: 輝度変更時に PowerShell を呼び出すため、わずかな遅延が発生する。

### 視覚バグ修正と AI OCR 妨害の再設計 (v0.1.13)
- **対角線グラデーションの修正**: `Uniforms` 構造体のアライメントを 16 バイト境界（80バイト）に厳密に合わせ、シェーダー内の斜め成分計算に `modulo` 演算を適用することで、画面全域に発生していた意図しない輝度勾配を解消。
- **AI OCR 妨害の刷新**: ユーザーから「縦縞に見える」と指摘された従来のパターンを廃止し、千鳥格子状（Staggered Grid）のマイクロアパーチャ配列を採用。視覚的ストレスを抑えつつ、OCR による文字認識を効果的に妨害する高周波テクスチャへ変更。
- **Subpixel UHD Jamming (プロトタイプ)**: OLED および LCD の各モードにおいて、サブピクセル単位での干渉ロジックを導入。正面からの視認性を維持しつつ、斜めからの遮蔽力を物理ピクセルレベルで強化する戦略を推進。

### Code Review v2.0 対応と品質向上 (v0.1.12)
- **ホットプラグ処理の堅牢化**: 新規ディスプレイ接続時に、カテゴリ検出 (`detect_display_category`) をオーバーレイ生成より前に行うよう順序を修正。これにより、最初のフレームから適切な PPI ベースのパラメータが適用されるようになった (#1)。
- **アニメーション判定の最適化**: 毎フレーム行っていた全ディスプレイの走査を廃止。状態変更時（メニュー操作、ホットプラグ、AI 検知）にのみ `calc_needs_animation` で再計算し、変数を保持する方式に変更 (#8)。
- **表示変更イベントのバッチ処理**: ディスプレイ変更イベントが短時間に連続して発生した場合、プロキシスレッドでこれらをまとめ（デバウンス相当）、1回の `UserEvent` としてメインループに通知するように改善 (#3)。
- **macOS メインスレッド安全性の向上**: `detect_panel_type` 内で Objc2 API を使用する際、`MainThreadMarker::new()` を使用して安全にメインスレッドであることを確認するよう修正 (#2)。
- **macOS システム情報のパース改善**: `system_profiler` から取得したディスプレイ ID が 16 進数（例: `0x...`）の場合でも正しくパースできるように修正 (#6)。
- **Windows 内蔵判定のヒューリスティック改善**: `INTERNAL` キーワードに加え、プライマリディスプレイかつ横幅が 2560px 以下の場合をノート PC 内蔵と推定するフォールバックを追加 (#5)。
- **セキュリティと安全性**: `osascript` でアラートを表示する際、文字列をエスケープするように修正 (#7)。

### 既知の問題 / 持ち越し
- **macOS クラムシェルモードの制限**: クラムシェルモード（ノート PC の蓋を閉じた状態）からの復帰時、座標情報の変化により `position_key` が一致せず、稀に設定が `Unknown` にリセットされる場合がある。長期的には `display_uuid` やシリアル番号ベースの識別に移行を検討 (#4)。
- **Windows 実機検証**: Windows 固有の `is_internal_display` フォールバックは、実機の多様な環境（特にマルチモニター環境）でのさらなる検証が必要。

## 2026-05-16

### Phase 5: LCD IPS 視野角コントラスト崩壊型フィルターの実装
- **新戦略の導入**: LCD IPS パネルの「斜め方向でコントラストが激減する」という物理特性を積極的に利用。黒い縞で隠すのではなく、微細な輝度変調によって斜めからの判読性を奪うアプローチを採用。
- **マルチレイヤー合成シェーダー**: 
    - 輝度圧縮マイクログリッド: 前景/背景の輝度差を圧縮。
    - 逆コントラスト斜線ハッチング: 文字エッジのMTFを物理的に破壊。
    - 動的位相ノイズ: 8Hz の輝度揺らぎでカメラ等の積分を妨害。
- **LCD IPS 最適化**: パネル種別が「LCD IPS」の場合、デフォルトの推奨モードとして `LcdContrastJammer` が自動選択されるように変更。
- **低輝度低下・高視認性**: 従来の黒縞フィルターに比べ、正面ユーザーの感じる画面の暗さを大幅に軽減（alpha 加算を 0.45 以下に抑制）。
- **物理サイズ基準のパラメータ導入**: フィルターの周期 (`period`) やスクロール速度をピクセル数ではなく物理ミリメートル単位で定義。これにより PPI の異なるディスプレイ間でも視覚的な遮蔽効果を一定に保つことが可能になった。
- **ディスプレイカテゴリの自動判定**: 内蔵/外付け、解像度、物理サイズから `NotebookFhd`, `NotebookHiDpi`, `ExternalLarge4K`, `ExternalGeneral` の4カテゴリを自動判定するロジックを実装。
- **プラットフォーム固有の実装**: 
    - macOS: `CGDisplayIsBuiltin` での内蔵判定、`CGDisplayScreenSize` での物理サイズ取得を実装。HiDPI スケーリング時（例: 4K モニターで 3008×1692 設定）でも正しく 4K 相当として判定できるよう、カテゴリ検出の閾値を調整。PPI 計算に用いる解像度が論理値（スケーリング後）であっても、最終的な視覚効果としての物理ミリメートル幅が一致することを確認。
    - Windows: `EnumDisplayDevices` での内蔵判定、`GetDeviceCaps` での物理サイズ取得を実装。
- **シェーダーの物理サイズ対応**: `Uniforms` を拡張し、PPI に応じて計算されたピクセル単位のパラメータをシェーダーへ渡すように変更。
- **UI 統合**: トレイメニューに現在の画面タイプと PPI を表示し、必要に応じて手動でカテゴリを上書きできる機能を追加。

## 2026-05-15

### Phase 0: プロトタイプ (macOS)
- **プロジェクト初期化**: `cargo init` を実行し、`Cargo.toml` に `tao`, `softbuffer`, `tray-icon`, `muda`, `global-hotkey`, `objc2` 等の依存関係を追加。
- **ディレクトリ構造の作成**: `src/` 直下に `app.rs`, `display_config.rs`, `overlay.rs`, `platform/` を作成し、初期ファイルを配置。
- **データ構造の実装**: `MonitorId`, `DisplayConfig`, `AppState` を実装。
- **オーバーレイウィンドウの実装**: `softbuffer` を使用した描画ロジックと、`objc2` を使用した macOS 固有設定（クリック透過、最前面、全スペース表示）を実装。
- **ビルド確認**: `cargo build` が正常に完了し、実行可能なバイナリが生成されることを確認。

### Phase 1: MVP (macOS)
- **単一インスタンス制御**: `src/single_instance.rs` を実装。macOS では `flock`、Windows ではミューテックスを使用。
- **トレイ UI**: `src/tray.rs` を実装。`tray-icon` と `muda` を使用し、グローバルトグル、ディスプレイ別設定、終了メニューを構築。
- **ホットプラグ対応**: macOS の `NSNotificationCenter` を利用して画面構成変更を検知し、オーバーレイウィンドウを動的に追加・削除するロジックを実装。
- **グローバルショートカット**: `src/hotkey.rs` を実装。`Cmd+Shift+P` でフィルターのグローバル ON/OFF を切り替え可能に。
- **macOS バンドル**: `Info.plist` を作成し、`.app` バンドルを生成するスクリプト `scripts/bundle_macos.sh` を用意。`LSUIElement` を有効にして Dock アイコンを非表示化。
- **Makefile**: `make mac`, `make win`, `make all` 等のターゲットを持つ `Makefile` を作成し、ビルド工程を自動化。

### 既知の問題 / 持ち越し
- **Windows 版の検証**: 現在の環境が macOS のため、Windows 特有の動作（トレイアイコン、ホットプラグ検知）は未検証。
- **アイコン**: 現時点では Python で生成したダミーの黒い PNG を使用。製品版では適切なデザインの icns/ico が必要。
- **softbuffer の再生成**: ホットプラグ時にウィンドウを再作成する際、描画サーフェスも適切に管理する必要がある（現在は都度生成）。

---

## 2026-05-15

### Phase 2: Ver 2.0 拡張 (macOS)
- **設定の永続化**: `confy` と `serde` を導入し、`AppConfig` を介して `AppState` の状態（グローバルON/OFF、濃度、各ディスプレイの設定、自動起動設定）を保存・復元するロジックを実装。
- **濃度変更 UI**: トレイメニューに「フィルター濃度」サブメニューを追加し、10%〜90%の間で一括変更できるようにした。
- **自動起動**: `auto-launch` クレートを使用し、macOS の LaunchAgent への登録・解除をトレイメニューから制御できるように実装。
- **macOS Alpha 調整**: 今まで固定値だった `setAlphaValue` を `AppState` の設定値に連動するように修正。

### 既知の問題 / 持ち越し
- **Windows 版の自動起動**: `auto-launch` は Windows のレジストリにも対応しているが、実機での動作確認が必要。
- **ルーバーパターンの色味**: 現在は 1px 黒 : 1px 透明の固定だが、Alpha 設定との兼ね合いで調整が必要になる可能性がある。

---

## 2026-05-15

### Phase 2 Extension: 縦縞ルーバーパターン (macOS)
- **tiny-skia 導入**: 描画ロジックの柔軟性を高めるため `tiny-skia` を追加。
- **描画モード追加**: 「単色レイヤー」と「縦縞ルーバー」を切り替える `FilterMode` を実装。
- **ルーバー描画**: 1px 黒 : 1px 透明の垂直ストライプを描画するロジックを `overlay.rs` に追加。
- **トレイメニュー更新**: フィルター形式を選択できるサブメニューを追加。設定は永続化される。

### Phase 3: AI 覗き見検知 (macOS)
- **技術選定**: `tract-onnx` と `nokhwa` を採用。Pure Rust に近い構成で軽量な推論環境を構築。
- **バックグラウンド検知**: 専用スレッドでカメラキャプチャと推論を実行。メインループへの通知には `mpsc` と `UserEvent` を使用。
- **動的フィルター**: 2人以上の顔を検知した際、一時的に Alpha を 0.8 に引き上げるロジックを実装。
- **権限管理**: `Info.plist` を更新し、macOS でのカメラ・アクセシビリティ権限の説明を追加。
## 2026-05-16

### Phase 5: バグ修正と視覚的妨害効果の更なる改善
- **シェーダーのバグ修正 (shader.wgsl)**:
    - `Unknown` パネルのスクロール速度が極低速（0.2px/s）になっていた問題を修正し、保守的な 300px/s に設定。
    - `FastVibration` モードが `bidirectional` フラグを無視していた問題を修正。
    - Rust 側と WGSL 側の `Uniforms` 構造体のアライメントとフィールド数（パディング）を一致させた。
- **イベントループの修正 (main.rs)**:
    - `Poll` モード中に `RedrawRequested` と `MainEventsCleared` で二重に描画が走るバグを修正。
- **プライバシー保護効果の強化 (shader.wgsl)**:
    - **輝度漸減レイヤー**: `VerticalLouver` 等の透明部分に薄いグレーを重畳し、斜め方向での輝度差を強調。
    - **ダブルレイヤー合成**: `FastVibration` に第2の干渉レイヤーを追加し、斜め方向でのモアレ（干渉縞）発生を促進。
    - **エッジブラー効果**: 縞の境界に 1.5px のグラデーションを追加し、正面からの視覚的ストレスを軽減しつつ、斜めからの輪郭をボカした。
    - **Intensity 仕様変更**: フィルター強度が「縞の太さ」ではなく「遮蔽率 (30% - 90%)」に直感的に連動するよう計算式を刷新。
    - **動的閾値**: `AsymmetricCurve` の遮蔽率を強度に応じて動的に変化させるよう改善。
- **シェーダーの修正 (shader.wgsl)**: 
    - `discard` を廃止し、透明色 (`vec4(0,0,0,0)`) と遮蔽色 (`vec4(0,0,0,alpha)`) を返すように変更。これによりフィルター濃度設定が正しく機能するようになった。
    - パネル種別（OLED/LCD）に応じた高速スクロール（300-600px/秒）と位相反転アニメーションを各モードに実装。
    - `bidirectional` フラグを導入し、OLED 等で格子状（四方向）の遮蔽パターンを適用可能にした。
- **イベントループの改善 (main.rs)**:
    - アニメーションが必要なモードが有効な際、`ControlFlow::Wait` から `ControlFlow::Poll` に自動的に切り替えるロジックを実装。
    - `MainEventsCleared` で描画を駆動することで、フレームレートの安定化と滑らかなアニメーションを実現。
- **データ構造の更新 (overlay.rs)**:
    - `Uniforms` 構造体に `bidirectional` フィールドを追加し、16バイト境界へのアライメント調整（パディング）を実施。
- **パネル判定ロジックの実装**: macOS (`system_profiler`, `NSScreen` EDR) および Windows (`EnumDisplayDevices`, モデル名マッチング) におけるパネル種別（OLED/LCD IPS/LCD TN）の自動判定を実装。
- **適応型フィルタ戦略**: シェーダー (`shader.wgsl`) を更新し、パネル種別に応じた最適化（OLED: 焼き付き防止、LCD IPS: 位相反転アニ メーション、LCD TN: 垂直スクロール）を適用。
- **自動モード切り替え**: パネル種別が判定・変更された際、そのパネルに最適な `FilterMode`（OLED: 縦縞, IPS: 高速振動, TN: 非対称曲線）を自動的に選択するロジックを `AppState` に実装。
- **UI 統合**: トレイメニューに判定されたパネル種別を表示し、手動での変更・保存を可能にした。
- **データ構造の更新**: `DisplayConfig` に `panel_type` を追加。
- **ドキュメント更新**: macOS での画面収録パーミッション要求に関する説明を `MANUAL.md` および仕様書に追加。


### 既知の問題 / 持ち越し
- **高度な画面加工**: `crabgrab` を導入したが、リアルタイムの Semantic Blur 等の実装は継続中。
- **視線追跡**: 基礎的な顔検知との統合を検討中。


