# Softveil Devlog

開発中に発生した問題・決定・気づきを時系列で記録する。  
**運用ルール**:
- 実装セッション終了時に追記する
- 「何を試みたか」「なぜそうしたか」「何がうまくいかなかったか」を残す
- 解決した既知の問題は仕様書 §9 の Known Issues テーブルも ✅ に更新する

---

## テンプレート（コピーして使う）

```
## YYYY-MM-DD

### <フェーズ名> (<対象 OS/範囲>)
- **<対応内容の見出し>**: <詳細>
- **<対応内容の見出し>**: <詳細>

### 既知の問題 / 持ち越し
- <項目>
```

---

<!-- 以下に実装ログを追記していく -->

## 2026-05-15

### Phase 0: プロトタイプ (macOS)
- **プロジェクト初期化**: `cargo init` を実行し、`Cargo.toml` に `tao`, `softbuffer`, `tray-icon`, `muda`, `global-hotkey`, `objc2` 等の依存関係を追加。
- **ディレクトリ構造の作成**: `src/` 直下に `app.rs`, `display_config.rs`, `overlay.rs`, `platform/` を作成し、初期ファイルを配置。
- **データ構造の実装**: `MonitorId`, `DisplayConfig`, `AppState` を実装。
- **オーバーレイウィンドウの実装**: `softbuffer` を使用した描画ロジックと、`objc2` を使用した macOS 固有設定（クリック透過、最前面、全スペース表示）を実装。
- **ビルド確認**: `cargo build` が正常に完了し、実行可能なバイナリが生成されることを確認。

### Phase 1: MVP (macOS)
- **単一インスタンス制御**: `src/single_instance.rs` を実装。macOS では `flock`、Windows ではミューテックスを使用。
- **トレイ UI**: `src/tray.rs` を実装。`tray-icon` と `muda` を使用し、グローバルトグル、ディスプレイ別設定、終了メニューを構築。
- **ホットプラグ対応**: macOS の `NSNotificationCenter` を利用して画面構成変更を検知し、オーバーレイウィンドウを動的に追加・削除するロジックを実装。
- **グローバルショートカット**: `src/hotkey.rs` を実装。`Cmd+Shift+P` でフィルターのグローバル ON/OFF を切り替え可能に。
- **macOS バンドル**: `Info.plist` を作成し、`.app` バンドルを生成するスクリプト `scripts/bundle_macos.sh` を用意。`LSUIElement` を有効にして Dock アイコンを非表示化。
- **Makefile**: `make mac`, `make win`, `make all` 等のターゲットを持つ `Makefile` を作成し、ビルド工程を自動化。

### 既知の問題 / 持ち越し
- **Windows 版の検証**: 現在の環境が macOS のため、Windows 特有の動作（トレイアイコン、ホットプラグ検知）は未検証。
- **アイコン**: 現時点では Python で生成したダミーの黒い PNG を使用。製品版では適切なデザインの icns/ico が必要。
- **softbuffer の再生成**: ホットプラグ時にウィンドウを再作成する際、描画サーフェスも適切に管理する必要がある（現在は都度生成）。

---

## 2026-05-15

### Phase 2: Ver 2.0 拡張 (macOS)
- **設定の永続化**: `confy` と `serde` を導入し、`AppConfig` を介して `AppState` の状態（グローバルON/OFF、濃度、各ディスプレイの設定、自動起動設定）を保存・復元するロジックを実装。
- **濃度変更 UI**: トレイメニューに「フィルター濃度」サブメニューを追加し、10%〜90%の間で一括変更できるようにした。
- **自動起動**: `auto-launch` クレートを使用し、macOS の LaunchAgent への登録・解除をトレイメニューから制御できるように実装。
- **macOS Alpha 調整**: 今まで固定値だった `setAlphaValue` を `AppState` の設定値に連動するように修正。

### 既知の問題 / 持ち越し
- **Windows 版の自動起動**: `auto-launch` は Windows のレジストリにも対応しているが、実機での動作確認が必要。
- **ルーバーパターンの色味**: 現在は 1px 黒 : 1px 透明の固定だが、Alpha 設定との兼ね合いで調整が必要になる可能性がある。

---

## 2026-05-15

### Phase 2 Extension: 縦縞ルーバーパターン (macOS)
- **tiny-skia 導入**: 描画ロジックの柔軟性を高めるため `tiny-skia` を追加。
- **描画モード追加**: 「単色レイヤー」と「縦縞ルーバー」を切り替える `FilterMode` を実装。
- **ルーバー描画**: 1px 黒 : 1px 透明の垂直ストライプを描画するロジックを `overlay.rs` に追加。
- **トレイメニュー更新**: フィルター形式を選択できるサブメニューを追加。設定は永続化される。

### Phase 3: AI 覗き見検知 (macOS)
- **技術選定**: `tract-onnx` と `nokhwa` を採用。Pure Rust に近い構成で軽量な推論環境を構築。
- **バックグラウンド検知**: 専用スレッドでカメラキャプチャと推論を実行。メインループへの通知には `mpsc` と `UserEvent` を使用。
- **動的フィルター**: 2人以上の顔を検知した際、一時的に Alpha を 0.8 に引き上げるロジックを実装。
- **権限管理**: `Info.plist` を更新し、macOS でのカメラ・アクセシビリティ権限の説明を追加。
## 2026-05-16

### Phase 5: ソフトウェア定義型プライバシーディスプレイ (SPD)
- **GPU レンダリング刷新**: `tiny-skia` / `softbuffer` から `wgpu` への移行を完了。各オーバーレイウィンドウが `wgpu::Surface` を持ち、独立した描画ループを構成。
- **WGSL シェーダーの実装**: `src/shader.wgsl` を作成し、単色、ルーバー、高速振動、非対称曲線の各モードをシェーダー側で実装。
- **高速動体マスキング**: 60fps 以上でのパターン振動を実装。フリッカー融合により正面からは透明度を維持しつつ、斜めからの視認性を低下させる手法を導入。
- **OLED 対応**: パネル種別に応じた焼き付き防止（Anti-Burn-in）ロジックをシェーダーに追加。わずかなピクセルシフトを時間経過で適用。
- **UI 設計の刷新 (STEP 4-6)**: ディスプレイごとに独立して「有効/形式/濃度」を設定できる階層型メニューを実装。`DisplayConfig` に `filter_mode` を追加し、永続化に対応。
- **macOS 表示名の改善**: `system_profiler` を利用して、トレイメニューにメーカー名や型番を表示するように改善。
- **リファクタリング**: `OverlayWindow` による `Arc<Window>` の所有権管理と、インポート構造の整理（`FilterMode` を `display_config.rs` へ集約）。
- **Windows クロスコンパイル環境の整備**: macOS 上で `make win` を実行可能にするため、`x86_64-pc-windows-gnu` ターゲットと `mingw-w64` ツールチェーンを導入。`.cargo/config.toml` にリンカ設定を追加。


### 既知の問題 / 持ち越し
- **画面キャプチャ統合**: `crabgrab` によるリアルタイム画面解析（Semantic Blur）は、パフォーマンスと依存関係の整理が必要なため、今後の課題とする。
- **視線追跡**: 外部ライブラリまたは AI モデルによる視線推定との統合。

